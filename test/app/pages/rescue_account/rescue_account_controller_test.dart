import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:spotify_clone/app/pages/rescue_account/rescue_account_controller.dart';
import 'package:spotify_clone/app/app_module.dart';

void main() {
  initModule(AppModule());
  // RescueAccountController rescueaccount;
  //
  setUp(() {
    //     rescueaccount = AppModule.to.get<RescueAccountController>();
  });

  group('RescueAccountController Test', () {
    //   test("First Test", () {
    //     expect(rescueaccount, isInstanceOf<RescueAccountController>());
    //   });

    //   test("Set Value", () {
    //     expect(rescueaccount.value, equals(0));
    //     rescueaccount.increment();
    //     expect(rescueaccount.value, equals(1));
    //   });
  });
}
