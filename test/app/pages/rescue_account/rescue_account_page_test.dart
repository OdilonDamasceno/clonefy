import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

main() {
  testWidgets('RescueAccountPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(RescueAccountPage(title: 'RescueAccount')));
    //  final titleFinder = find.text('RescueAccount');
    //  expect(titleFinder, findsOneWidget);
  });
}
